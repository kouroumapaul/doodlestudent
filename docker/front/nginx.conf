server {
    listen 80;
    server_name doodle.paulkourouma.com myadmin.paulkourouma.com pad.paulkourouma.com;
    
    # Configuration pour Let's Encrypt
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    
    # Redirection vers HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name doodle.paulkourouma.com;
    
    ssl_certificate /etc/letsencrypt/live/doodle.paulkourouma.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/doodle.paulkourouma.com/privkey.pem;
    
    # Proxy vers le backend
    location /api {
        proxy_pass http://backend:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # Servir les fichiers statiques du frontend
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
}

server {
    listen 443 ssl;
    server_name pad.paulkourouma.com;
    
    ssl_certificate /etc/letsencrypt/live/doodle.paulkourouma.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/doodle.paulkourouma.com/privkey.pem;
    
    # Proxy vers Etherpad
    location / {
        proxy_pass http://etherpad:9001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 443 ssl;
    server_name myadmin.paulkourouma.com;
    
    ssl_certificate /etc/letsencrypt/live/doodle.paulkourouma.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/doodle.paulkourouma.com/privkey.pem;
    
    # Proxy vers PHPMyAdmin
    location / {
        proxy_pass http://myadmin:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Ajoutez ces blocs server à votre fichier nginx.conf existant

server {
    listen 80;
    server_name grafana.paulkourouma.com munin.paulkourouma.com;
    
    # Configuration pour Let's Encrypt
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    
    # Redirection vers HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name grafana.paulkourouma.com;
    
    ssl_certificate /etc/letsencrypt/live/doodle.paulkourouma.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/doodle.paulkourouma.com/privkey.pem;
    
    # Proxy vers Grafana
    location / {
        proxy_pass http://grafana:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 443 ssl;
    server_name munin.paulkourouma.com;
    
    ssl_certificate /etc/letsencrypt/live/doodle.paulkourouma.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/doodle.paulkourouma.com/privkey.pem;
    
    # Proxy vers Munin
    location / {
        root /var/cache/munin/www;
        index index.html index.htm;
    }
}